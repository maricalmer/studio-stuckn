name: Frontend checks
on:
  pull_request:
  push:
    branches: [master, main]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            studio/package-lock.json
      - run: npm install --global npm@11.16.0
      - run: npm ci
      - run: npm ci --prefix studio
      - run: npm run sanity:typegen:check
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:content
      - run: npm run build
        env:
          # These are public Sanity project settings, not secrets. GitHub Actions
          # does not inherit the environment configured for the Netlify site.
          NEXT_PUBLIC_SANITY_PROJECT_ID: 35ex4ltc
          NEXT_PUBLIC_SANITY_DATASET: production
          NEXT_PUBLIC_SANITY_STUDIO_URL: https://ronjastucken.sanity.studio
